services:
  v2bx:
    image: crpi-r21td9yeapqngs0l.cn-chengdu.personal.cr.aliyuncs.com/coterjiesen/v2bx:0.0.2  # You can specify a different version if needed
    container_name: v2bx

    ports:
      - "443:443" 
    volumes:
      - ./config.json:/opt/config.json
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/cert:/etc/nginx/cert
      - ./nginx/conf.d:/etc/nginx/conf.d
      # - ./nginx/html:/etc/nginx/html
